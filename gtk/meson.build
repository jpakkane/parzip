# Distro packagers, do not install this executable,
# it is not production ready.
executable('parzip-gtk', 'parzip-gtk.cpp',
  include_directories : libinc,
  link_with : zl,
  dependencies : [gtkdep, threaddep])
